apiVersion: v1
kind: Service
metadata:
  name: fraud-detection
  namespace: fraud-detection
  labels:
    app: fraud-detection
spec:
  type: ClusterIP
  selector:
    app: fraud-detection
    component: api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
